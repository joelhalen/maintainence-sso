# Copy to .env.capacitor.local or export before build-android-apk.sh
# API base URL for the bundled Android APK (must be reachable from mobile networks)
VITE_API_URL=https://megamtx.joelhalen.net/api
VITE_CAPACITOR_BUILD=true
